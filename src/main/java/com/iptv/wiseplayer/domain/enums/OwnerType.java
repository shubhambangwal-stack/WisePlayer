package com.iptv.wiseplayer.domain.enums;

public enum OwnerType {
    ADMIN,
    RESELLER,
    SUB_RESELLER,
    SYSTEM,
    DEVICE // When directly created by a user device without an upstream owner
}
