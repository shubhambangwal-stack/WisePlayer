package com.iptv.wiseplayer.domain.enums;

/**
 * Defines the type of subscription associated with a device.
 */
public enum SubscriptionType {
    TRIAL,
    MONTHLY,
    PAID_ANNUAL,
    PAID_LIFETIME,
    ANNUAL,
    EXPIRED
}
